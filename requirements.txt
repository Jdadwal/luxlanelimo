# Luxlane runs on the Python standard library for everything except the optional
# Postgres backend. psycopg2-binary is only imported when DATABASE_URL is set,
# so the app still runs with zero third-party packages when no database is used.
psycopg2-binary>=2.9
